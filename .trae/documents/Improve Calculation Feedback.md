# 计算完成交互优化

根据您的需求，我优化了“开始计算最优方案”后的交互体验：

1.  **轻量提示**：计算完成后，屏幕下方会弹出一个黑色的“计算完成！”提示（Toast），让您明确知道系统已经处理完毕。
2.  **自动滚动**：页面会自动平滑滚动到**方案 #1**的位置，您无需手动向下滑动即可直接看到第一个最优方案。

**代码变更**：
-   `js/stats.js`：在 `calculateLayout` 函数末尾添加了 `showToast` 调用和 `scrollIntoView` 逻辑。

请刷新页面，输入规格并点击计算按钮体验一下流畅的效果。